# [INC-2026-08-26-01] 구매 요청 API 503 장애 기록

## 1. 장애 개요

| 항목 | 내용 |
|---|---|
| 상태 | 원인 조치 완료, 인증 포함 E2E 재검증 필요 |
| 발생 환경 | 로컬 Docker Compose 개발 환경 |
| 발생 일자 | 2026-08-26 |
| 장애 지속 시간 | 미기록 |
| 영향 기능 | 교보재 구매 요청 등록 |
| 사용자 증상 | 구매 요청 버튼을 누르면 `503 Service Unavailable` 발생 |
| 요청 경로 | `POST /api/enrollments/purchases` |
| 장애 서비스 | Request Service, 소스 모듈명 `enrollment-service` |
| 연관 서비스 | Asset Service `8082`, Budget Service `8084` |

### 한 줄 요약

Request Service가 Asset Service를 호출할 때 Eureka 서비스 조회 방식과 Docker DNS 직접 호출 방식을 혼용하여 대상 서비스를 찾지 못했고, 내부 예외가 503 응답으로 변환되었다.

## 2. 영향 범위

- 구매 요청 등록 API가 실패했다.
- 브라우저에서는 Axios 호출 위치인 `enrollment.js:11`에 503 오류가 표시되었다.
- 프론트엔드가 요청을 보내는 동작 자체는 정상이었다.
- DB, Kafka, 다른 화면의 기능 장애까지 발생했다는 증거는 확인되지 않았다.

> `컨테이너가 Up 상태`라는 사실은 프로세스가 실행 중이라는 뜻이다. 서비스 간 주소, 포트, 인증, API 경로가 정상이라는 뜻은 아니다.

## 3. 정상 흐름과 장애 흐름

### 정상 흐름

```text
Vue :3000
  → API Gateway :8080
  → Request Service :8083
  → Asset Service :8082
  → Budget Service :8084
```

### 장애 당시 흐름

```text
POST /api/enrollments/purchases
  → EnrollmentService.requestPurchase()
  → CourseServiceClient.createPurchaseCourse()
  → http://asset-service/api/courses 호출
  → @LoadBalanced WebClient가 asset-service를 Eureka 서비스 ID로 조회
  → Eureka에는 COURSE-SERVICE로 등록되어 있어 인스턴스를 찾지 못함
  → RuntimeException 발생
  → GlobalExceptionHandler가 예외를 HTTP 503으로 변환
  → 브라우저에 503 Service Unavailable 표시
```

## 4. 탐지 증상

브라우저에서 다음 오류가 반복적으로 발생했다.

```text
POST http://localhost:3000/api/enrollments/purchases
503 (Service Unavailable)
```

503은 프론트엔드가 만든 오류가 아니라, Request Service가 다음 내부 서비스를 호출하지 못한 결과를 서버에서 반환한 것이다.

## 5. 원인 분석

### 5.1 직접 원인

`CourseServiceClient`는 `http://asset-service/...`를 사용했고, `WebClient.Builder`에는 `@LoadBalanced`가 적용되어 있었다. 이 조합에서는 `asset-service`가 Docker 컨테이너명이 아니라 Eureka 서비스 ID로 해석된다.

하지만 해당 서비스는 Eureka에 `COURSE-SERVICE`로 등록되어 있었다. 호출 이름과 등록 이름이 달라 Request Service가 Asset Service 인스턴스를 찾지 못했다.

### 5.2 기여 요인

| 기여 요인 | 설명 |
|---|---|
| 포트 누락 | 기존 URL에 Asset Service의 내부 포트 `8082`가 없었다. 일반 HTTP 직접 호출로 처리하면 기본 포트 `80`을 사용하게 된다. |
| URL 하드코딩 | Asset Service URL 5곳과 Budget Service URL이 Java 코드에 직접 작성되어 있었다. |
| 서비스명 불일치 | Docker 이름은 `asset-service`, Eureka 등록명은 `COURSE-SERVICE`여서 호출 규칙이 명확하지 않았다. |
| 환경 설정 혼재 | 로컬 주소와 Docker 내부 주소가 설정 파일과 코드에 섞여 있었다. |
| 통합 검증 부족 | 컨테이너 기동 확인은 했지만 Request → Asset, Request → Budget 호출 검증이 없었다. |

### 5.3 원인이 아니었던 지점

- Axios 자체 오류가 아니다. Axios는 서버가 반환한 503을 표시했다.
- Vue 개발 서버 `3000` 포트 문제가 아니다. 요청은 Gateway와 Request Service까지 전달됐다.
- 단순히 Docker 컨테이너가 종료되어서 발생한 문제는 아니다.

## 6. 조치 내용

내부 서비스 호출 방식을 Docker DNS와 환경별 URL 설정 방식으로 통일했다.

| 위치 | 기존 문제 | 조치 |
|---|---|---|
| Request → Asset | `@LoadBalanced`가 `asset-service`를 Eureka ID로 해석 | 일반 `WebClient`로 변경 |
| Request → Asset | 포트가 없는 하드코딩 URL 5곳 | `${service.asset-service.url}` 사용 |
| Request → Budget | `budget-service:8084` 하드코딩 | `${service.budget-service.url}` 사용 |
| Asset → Member | `member-service`에 `8081` 포트 누락 | `MEMBER_SERVICE_URL` 사용 |
| Java 서비스 포트 | 실행 환경별 포트 변경이 어려움 | `SERVER_PORT`와 기본값 사용 |
| Java 인프라 설정 | DB, Kafka, Eureka의 로컬 주소와 Docker 주소 혼재 | 로컬 기본값과 Compose 환경변수로 분리 |
| Alternative → Auth | 로컬 설정에서 Docker 호스트명 사용 | 로컬 기본값을 `localhost:9000`으로 변경 |

### 로컬 실행 기본값

| 구성 요소 | 주소 |
|---|---|
| Member Service | `localhost:8081` |
| Asset Service | `localhost:8082` |
| Request Service | `localhost:8083` |
| Budget Service | `localhost:8084` |
| Alternative Service | `localhost:8085` |
| MariaDB | `localhost:3379` |
| Kafka | `localhost:9092` |
| Eureka | `localhost:8761` |
| Auth Server | `localhost:9000` |

### Docker 내부 설정

```yaml
ASSET_SERVICE_URL: http://asset-service:8082
BUDGET_SERVICE_URL: http://budget-service:8084
MEMBER_SERVICE_URL: http://member-service:8081
```

### 애플리케이션 기본 설정

```yaml
service:
  asset-service:
    url: ${ASSET_SERVICE_URL:http://localhost:8082}
  budget-service:
    url: ${BUDGET_SERVICE_URL:http://localhost:8084}
```

따라서 같은 코드를 환경별로 다음처럼 실행할 수 있다.

```text
로컬 실행
Request → localhost:8082 → Asset
Request → localhost:8084 → Budget

Docker 실행
Request → asset-service:8082 → Docker DNS → Asset
Request → budget-service:8084 → Docker DNS → Budget
```

## 7. 검증 결과

| 검증 단계 | 결과 | 해석 |
|---|---|---|
| 소스 확인 | 통과 | 일반 `WebClient`와 환경변수 기반 URL 사용 확인 |
| Compose 문법 | 통과 | `docker compose config --quiet` 종료 코드 `0` |
| 이미지 빌드 | 통과 | 관련 서비스 이미지 빌드 성공, 단 테스트는 `-x test`로 제외 |
| 컨테이너 실행 | 통과 | Compose 서비스가 모두 `Up`, 인프라 및 Auth, Eureka는 `healthy` |
| Request → Asset | 통과 | 컨테이너 내부에서 `GET http://asset-service:8082/api/courses`가 HTTP `200` 반환 |
| Request → Budget | 연결 통과 | `GET http://budget-service:8084/api/payments/internal/request`가 HTTP `405` 반환. 네트워크와 경로는 정상이며 GET이 허용되지 않는 엔드포인트라는 의미 |
| 인증 포함 구매 요청 E2E | 미완료 | 로그인 후 실제 구매 요청 POST를 다시 수행해야 최종 기능 검증 완료 |
| 자동화 테스트 | 미실행 | 빌드 시 테스트를 제외했으므로 별도 실행 필요 |

검증 시각: 2026-08-26 19:14 KST 기준으로 Asset 연결 HTTP 200과 Budget 연결 HTTP 405를 다시 확인했다.

## 8. 재현 및 점검 명령어

```bash
# 전체 컨테이너 상태 확인
docker compose ps

# Request Service 오류 로그 확인
docker compose logs --tail=200 request-service

# Compose 설정 문법 확인
docker compose config --quiet

# Request 컨테이너에서 Asset 연결 확인, 정상 기대값: HTTP 200
docker exec gearhub-request wget -q -S -O /dev/null \
  http://asset-service:8082/api/courses

# Request 컨테이너에서 Budget 연결 확인, GET 사용 시 기대값: HTTP 405
docker exec gearhub-request wget -q -S -O /dev/null \
  http://budget-service:8084/api/payments/internal/request
```

## 9. 재발 방지 항목

| 우선순위 | 조치 항목 | 완료 조건 | 담당자 | 기한 |
|---|---|---|---|---|
| P0 | 인증 포함 구매 요청 E2E 재검증 | 브라우저 구매 요청이 2xx 응답을 받고 Asset, Budget 데이터가 기대대로 변경됨 | TBD | TBD |
| P1 | 서비스 간 통합 테스트 추가 | Request → Asset → Budget 성공, 연결 실패 시나리오가 CI에서 검증됨 | TBD | TBD |
| P1 | 하드코딩 URL 방지 | Java 코드에서 `http://서비스명:포트` 직접 사용이 없고 설정 키로 통일됨 | TBD | TBD |
| P2 | 서비스 명명 규칙 통일 | Docker 서비스명, Spring application name, Eureka ID의 대응표가 문서화됨 | TBD | TBD |
| P2 | 장애 시각과 복구 시각 기록 | 다음 장애부터 최초 탐지, 조치 시작, 복구 완료 시각을 KST로 기록함 | TBD | TBD |

## 10. 회고

이번 장애의 핵심은 “포트 하나를 잘못 적었다”보다 “서비스를 찾는 방식과 이름 규칙이 통일되지 않았다”는 점이다. MSA에서는 컨테이너 실행 상태만 확인하지 않고, 실제 호출 주체 컨테이너에서 대상 서비스의 DNS, 포트, API 응답까지 확인해야 한다.

## 11. 별도 장애로 분리할 항목

로그인 화면에서 발생한 503은 Gateway가 시작 직후 Eureka에서 `AUTH-SERVER`를 찾지 못한 기동 순서 및 서비스 디스커버리 갱신 문제였다. 구매 요청 503과 HTTP 상태 코드는 같지만 원인이 다르므로, 현업에서는 별도의 장애 번호와 문서로 관리한다.
