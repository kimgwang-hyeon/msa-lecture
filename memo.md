# GearHub 기획 및 Agile 개발 정리

## 1. 서비스 한 줄 정의

교육기관이나 기업 구성원이 보유 교보재와 가용 수량을 직접 확인하고 대여와 구매를 표준화된 방식으로 요청하며, 운영진이 재고와 예산을 일관되게 검토할 수 있는 내부 장비 운영 서비스다.

현재 구현 범위는 여러 조직이 동시에 가입해 사용하는 멀티테넌트 SaaS가 아니라, SKALA 한 조직을 대상으로 핵심 가치를 검증하는 단일 테넌트 MVP다.

## 2. Pain Point

### 교육생, 일반 구성원

- 조직이 어떤 교보재를 보유하고 있는지 한눈에 알기 어렵다.
- 현재 대여할 수 있는 수량을 실시간으로 확인하기 어렵다.
- 대여 신청 방법이 구두, 메신저, 엑셀 등으로 분산되어 있다.
- 필요한 장비가 없을 때 구매 요청 양식이 사람마다 다르다.
- 비슷한 장비가 이미 있어도 이를 알지 못하고 새 구매를 요청할 수 있다.

### 자산 운영진, 학생회, 동아리 운영진

- 재고 조사, 신청, 승인, 반납 기록을 엑셀이나 수기로 관리해야 한다.
- 수기 관리 과정에서 기록 누락, 오기입, 중복 승인이 발생할 수 있다.
- 대여 승인 후 실제 재고 수량과 문서상 수량이 달라질 수 있다.
- 물품의 위치, 수량, 상태와 신청 이력을 한 화면에서 추적하기 어렵다.
- 담당자가 바뀌면 기존 관리 방식과 기록이 제대로 인수인계되지 않을 수 있다.
- 자산 변경 이력을 남기기 어려워 분실, 도난, 횡령과 같은 문제를 추적하기 어렵다.

### 예산 승인자

- 상품 링크, 단가, 수량, 구매 사유가 서로 다른 문서로 전달된다.
- 총액을 수기로 계산하면 오류가 발생할 수 있다.
- 이미 보유한 대체 장비가 있는지 확인하기 어렵다.
- 승인과 반려 결과가 구매 요청 기록에 자동으로 반영되지 않는다.

### 핵심 문제 정의

장비가 없는 것이 문제가 아니라, 이미 보유한 장비의 정보와 신청, 승인, 재고, 예산 기록이 여러 수단에 흩어져 있다는 것이 핵심 문제다.

GearHub는 이 과정을 한 시스템에서 시각화하고 표준화하여 불필요한 수작업을 줄이고, 모든 변경 이력을 추적할 수 있도록 한다.

## 3. 도메인을 도출한 과정

서비스명에서 바운디드 컨텍스트를 역산하지 않는다. 먼저 실제 업무 흐름을 이벤트 스토밍으로 펼치고, 함께 변경되는 정책과 데이터를 묶어 컨텍스트를 도출한 뒤 기존 기술 서비스에 매핑한다.

### 주요 액터

- 교육생, 일반 구성원
- 교육단체 관리자
- 교보재 담당자, 자산 운영진
- 예산 승인자
- 시스템 관리자

관리자는 별도의 컨텍스트라기보다 사용자에게 부여되는 역할로 본다.

### 이벤트 스토밍으로 확인한 전체 업무

```text
교육단체가 등록됨
→ 담당자가 등록됨
→ 교보재가 등록됨
→ 재고가 입고됨
→ 사용자가 대여를 신청함
→ 운영진이 대여를 승인하거나 반려함
→ 승인된 요청이 활성화됨
→ 가용 재고가 감소함
```

현재 2일 MVP 이후의 확장 흐름은 다음과 같다.

```text
대여 가능 여부가 기간별로 확인됨
→ 교보재가 예약됨
→ 교보재가 출고됨
→ 반납 예정일이 지남
→ 교보재가 반납됨
→ 물품 상태가 점검됨
→ 재고가 다시 사용 가능해짐
```

구매 흐름은 다음과 같다.

```text
필요한 교보재가 없음
→ 보유 중인 대체재를 확인함
→ 신규 구매를 요청함
→ 서버가 총액을 계산함
→ 예산 검토가 생성됨
→ 운영진이 승인하거나 반려함
→ Kafka 이벤트가 발행됨
→ 구매 요청 상태가 자동 변경됨
```

실패와 예외 이벤트도 함께 고려한다.

- 대여가 반려되거나 취소됨
- 가용 재고가 부족함
- 중복 승인이 시도됨
- 예산이 반려됨
- Kafka 이벤트가 중복 전달됨
- 물품이 연체됨
- 물품이 파손되거나 분실됨

### 명령, 정책, 이벤트

| 명령 | 정책 및 조건 | 발생 이벤트 |
|---|---|---|
| 교보재 등록 | 자산번호와 바코드는 중복될 수 없음 | `MaterialRegistered` |
| 대여 신청 | 조직 소속 사용자만 신청할 수 있음 | `RentalRequested` |
| 대여 승인 | PENDING 요청이고 가용 재고가 있어야 함 | `RentalApproved` |
| 재고 감소 | 승인된 요청에 대해서만 한 번 수행 | `StockDecreased` |
| 구매 요청 | 상품명, 링크, 단가, 수량, 사유가 필요함 | `PurchaseRequested` |
| 예산 생성 | 총액은 서버에서 단가 × 수량으로 계산 | `BudgetRequested` |
| 예산 승인 | PENDING 예산만 승인 가능 | `BudgetApproved` |
| 예산 반려 | 반려 사유를 기록해야 함 | `BudgetRejected` |
| 반납 처리, 향후 | 출고된 대여만 반납할 수 있음 | `RentalReturned` |
| 상태 점검, 향후 | 파손 물품은 즉시 대여할 수 없음 | `MaterialRepairRequired` |

## 4. 바운디드 컨텍스트와 서비스 매핑

업무 규칙과 변경 이유가 같은 것끼리 묶어 다음 컨텍스트를 식별했다.

| 도메인 컨텍스트 | 핵심 책임 | 기존 기술 서비스 | 현재 논리 서비스명 |
|---|---|---|---|
| Member Context | 사용자, 조직, 역할 관리 | `user-service` | Member Service |
| Asset Context | 교보재 정보, 자산가치, 전체 수량, 가용 수량 | `course-service` | Asset Service |
| Request Context | 대여와 구매 요청, 승인 상태 전이 | `enrollment-service` | Request Service |
| Budget Context | 구매 금액 계산, 예산 승인과 반려 | `payment-service` | Budget Service |
| Alternative Context | 구매 전 대여 가능한 대체재 조회 | `recommend-service` | Alternative Service |

Auth, Gateway, Eureka, Kafka, MariaDB는 업무 바운디드 컨텍스트가 아니라 공통 인프라로 본다.

기존 Gateway와 소스의 호환성을 위해 다음 기술 계약은 유지한다.

- 기존 소스 디렉터리와 Java 패키지
- 기존 물리 테이블 이름
- `/api/courses`, `/api/enrollments`, `/api/payments` 경로
- `payment.completed`, `enrollment.completed` Kafka 토픽
- 기존 포트

즉, 새로운 서비스를 전부 다시 만든 것이 아니라 기존 기술 계약은 유지하면서 도메인 의미를 GearHub에 맞게 치환했다.

### 컨텍스트별 핵심 규칙

#### Member Context

- 사용자는 조직에 소속되고 역할에 따라 기능에 접근한다.
- `ORG_ADMIN`, `ASSET_MANAGER`, `BORROWER` 같은 업무 역할은 Member가 관리한다.
- Auth Server는 로그인과 신원 확인을 담당한다.
- 사용자가 탈퇴해도 과거 대여와 구매 이력은 삭제하지 않는다.

#### Asset Context

- 자산번호와 바코드는 중복될 수 없다.
- 전체 수량과 가용 수량을 구분한다.
- 가용 수량은 전체 수량을 넘을 수 없다.
- 승인된 대여 요청에 대해서만 가용 수량을 감소시킨다.
- 향후 파손, 수리, 분실 상태를 관리한다.

#### Request Context

- 요청 유형은 대여와 구매로 구분한다.
- 대여 신청은 먼저 `PENDING` 상태로 생성한다.
- 운영진 승인 후 재고가 정상적으로 감소해야 `ACTIVE`로 전환한다.
- 반려된 요청에는 반려 사유를 남긴다.
- Budget 결과 이벤트를 받아 구매 요청 상태를 `ACTIVE` 또는 `REJECTED`로 변경한다.

#### Budget Context

- 현재 요구사항에는 실제 결제가 없으므로 결제 서비스가 아니라 예산 검토 컨텍스트로 사용한다.
- 구매 총액은 클라이언트가 아니라 서버가 계산한다.
- PENDING 예산만 승인하거나 반려할 수 있다.
- 승인과 반려 결과를 Kafka 이벤트로 발행한다.
- 향후 보증금, 연체료, 파손 배상금이 범위에 포함되면 Settlement Context로 확장할 수 있다.

#### Alternative Context

- 구매하려는 물품과 같은 카테고리의 보유 자산을 조회한다.
- 보유 상태이고 가용 수량이 있는 장비만 대체재로 제시한다.
- 현재 구현은 AI 추천이 아니라 규칙 기반 조회다.
- Alternative가 중단되어도 핵심 대여 흐름은 동작해야 한다.
- 다른 서비스의 데이터를 직접 수정하지 않는다.

### 서비스 간 데이터 공유 원칙

서비스끼리 JPA 엔티티나 전체 테이블을 직접 공유하지 않고 식별자와 이벤트를 전달한다.

```text
userId
organizationId
materialId
assetId
requestId
budgetId
```

예를 들어 Asset의 JPA 엔티티를 Request가 직접 참조하거나 같은 테이블을 조인하지 않는다.

## 5. BM, Business Model

### 초기 고객

- 학생회, 총학생회
- 동아리 연합회와 산하 동아리
- 교육기관과 교육 과정 운영 조직
- 사내 교육팀, 장비를 공동 관리하는 기업 조직

### 고객과 사용자의 구분

- 결제 고객은 총학생회, 교육기관, 기업처럼 상위 조직이다.
- 실제 사용자는 운영진, 자산 관리자, 일반 구성원 같은 하위 집단이다.
- 상위 조직이 서비스를 도입하면 여러 하위 집단에 배포하고 역할별로 사용할 수 있게 하는 구조를 목표로 한다.

### 제공 가치

- 보유 자산과 가용 수량을 한눈에 시각화한다.
- 신청, 승인, 재고 차감, 예산 검토 과정을 표준화한다.
- 운영자 개인의 엑셀과 메신저에 의존하지 않고 조직의 기록으로 남긴다.
- 변경 이력을 추적해 분실, 도난, 오기입 위험을 낮춘다.
- 구매 전에 기존 대체재를 보여줘 불필요한 중복 구매를 줄인다.
- 담당자가 교체되어도 동일한 운영 프로세스를 유지할 수 있다.

### 확장 가능한 판매 구조

초기에는 한 조직을 대상으로 MVP를 검증하고, 이후 상위 조직이 여러 하위 집단을 관리하는 구조로 확장한다.

```text
총학생회, 교육기관, 기업
└── 운영 조직
    ├── 자산 관리자
    ├── 예산 승인자
    └── 일반 구성원
```

수익 모델은 아직 검증 전 가설로 구분한다.

- 조직 단위 월간 또는 연간 구독
- 관리 자산 수와 사용자 수에 따른 요금제
- 초기 데이터 이전과 조직별 운영 설정 비용
- 대규모 기관용 구축, 유지보수, 감사 리포트 기능

현재 MVP의 목표는 요금제를 확정하는 것이 아니라, 대여와 구매 승인 과정의 수작업이 실제로 줄어드는지 검증하는 것이다.

## 6. Agile 운영 방법

전체 개발 흐름은 다음과 같다.

```text
Pain Point와 이해관계자 정의
→ 이벤트 스토밍
→ 바운디드 컨텍스트 도출
→ 기존 서비스에 매핑
→ Product Backlog 작성
→ Sprint Planning
→ 구현과 Daily Scrum
→ Sprint Review
→ Sprint Retrospective
→ Review 결과로 Backlog 재정렬
```

문서에서는 증거 수준을 다음과 같이 구분한다.

- `CODE EVIDENCE`: 코드와 테스트로 확인한 사실
- `RECONSTRUCTED`: 현재 결과를 기반으로 복원한 계획과 Daily 초안
- `TEAM INPUT`: 실제 팀원 발언과 외부 피드백
- `FUTURE`: 아직 구현하지 않은 후속 기능

현재 코드 상태를 바탕으로 Sprint Board와 Daily 진행 상황을 재구성하되, 실제로 수행하지 않은 회의나 외부 피드백을 수행한 것처럼 작성하지 않는다.

## 7. Sprint 1, 대여 워킹 스켈레톤

### Sprint Goal

교육생이 로그인해 보유 교보재와 가용 수량을 확인하고 대여를 신청하면, 운영진이 승인하여 신청 활성화와 재고 차감까지 완료되는 워킹 스켈레톤을 만든다.

### 포함 범위

- Member
- Asset
- Request
- 대여 관련 Frontend
- 기존 Auth, Gateway, Eureka

### 제외 범위

- Budget
- Kafka를 이용한 구매 상태 전파
- Alternative
- 반납
- 날짜별 예약

### 사용자 흐름

```text
교육생 로그인
→ 교보재 목록 조회
→ 상세 정보와 가용 수량 확인
→ 사용 목적을 입력해 대여 신청
→ Request PENDING
→ 운영진이 승인 대기 요청 조회
→ 운영진 승인
→ Asset 가용 재고 2에서 1로 감소
→ Request ACTIVE
```

Sprint 1은 PENDING 요청을 생성하는 것에서 끝나지 않는다. 운영진 승인, ACTIVE 전환, 실제 재고 감소까지 확인해야 Done이다.

### Sprint 1 Backlog

- G-01: 기존 교육 도메인을 GearHub 도메인으로 매핑
- M-01~02: OAuth2 로그인과 사용자 역할 표시
- A-01: 보유 교보재 목록과 가용 수량 조회
- A-02: 교보재 상세와 자산가치 조회
- A-03: 운영진 교보재 등록
- R-01: 사용 목적이 포함된 대여 신청
- R-02: 운영진 승인 대기 목록
- R-03: 승인, 재고 차감, ACTIVE 전환
- R-04: 반려와 반려 사유

계획량은 24SP다.

### Sprint 1의 주요 결정

- 전체 Spring Context Test와 순수 도메인 테스트를 분리한다.
- 대여 E2E 흐름 밖의 기능은 Sprint 1에 추가하지 않는다.
- 기존 Gateway URL과 기술 계약을 유지한다.
- 요청 생성뿐 아니라 승인 후 재고 감소까지 확인해야 Done이다.

### Sprint Review 시나리오

1. 교육생이 로그인한다.
2. iPhone 가용 수량이 2개인 것을 확인한다.
3. 사용 목적을 입력하고 대여를 신청한다.
4. 운영진 화면에서 PENDING 요청을 확인한다.
5. 운영진이 요청을 승인한다.
6. Request가 ACTIVE로 변경된 것을 확인한다.
7. 가용 수량이 2에서 1로 감소한 것을 확인한다.

문서 기준으로 Sprint 1의 계획 24SP는 모두 완료했다.

### Sprint 1 Review 이후 결정

- 기존 Payment 구조는 실제 결제가 아니라 예산 검토에 활용한다.
- 새 물품을 구매하기 전에 보유 대체재를 먼저 보여준다.
- Kafka는 예산 승인과 반려 결과를 Request에 자동 반영하는 데 사용한다.
- 반납보다 불필요한 신규 구매를 줄이는 흐름을 먼저 검증한다.

### Sprint 1 Retrospective

| 구분 | 내용 |
|---|---|
| Keep | 기존 API와 테이블을 재사용해 하루 안에 수직 흐름을 완성함 |
| Problem | Course, Enrollment 같은 기존 용어가 새 도메인 설명을 방해함 |
| Problem | DB 의존 테스트가 Docker 환경에 강하게 결합됨 |
| Try | 외부 설명과 UI는 Asset, Request, Budget 용어로 통일함 |
| Try | 단위 테스트와 Docker 통합 테스트를 분리함 |
| Try | Sprint 2에서 구매 전 대체재 확인을 추가함 |

## 8. Sprint 2, 구매, 대체재, 예산, Kafka

### Sprint Goal

보유하지 않은 교보재를 구매 요청하면 기존 대체재 확인과 예산 검토를 거치고, 승인과 반려 결과가 Kafka를 통해 구매 신청 상태에 자동 반영되는 흐름을 완성한다.

### 사용자 흐름

```text
구매 요청 카테고리 선택
→ Alternative 대체재 조회
→ 상품명, 링크, 단가, 수량, 사유 입력
→ 구매 요청 생성
→ 서버에서 단가 × 수량 계산
→ Budget PENDING
→ 운영진 승인 또는 반려
→ payment.completed 이벤트 발행
→ Request ACTIVE 또는 REJECTED
```

Sprint 2는 반납이나 기간 예약 구현이 아니다. 신규 구매 요청, 대체재 확인, 예산 승인과 반려, Kafka 상태 반영이 핵심이다.

### Sprint 2 Backlog

- X-01: 동일 카테고리의 가용 대체재 조회
- X-02: 신규 구매 요청 생성
- X-03: 대체재 확인을 구매 요청의 선행 단계로 사용
- B-01: 서버 계산 총액과 Budget PENDING 생성
- B-02: 운영진 예산 승인과 반려
- E-01: Kafka 결과로 Request 상태 자동 변경
- F-01: 내 신청 상태와 반려 사유 표시
- N-01: 새 논리 서비스명으로 실행 확인
- N-02: 빌드와 통합 검증 기록
- D-01: 실제 화면 증거 수집

계획량은 30SP다.

### Sprint 2의 주요 결정

- Gateway 이미지와 기존 API 경로는 수정하지 않는다.
- 논리 서비스명과 Eureka, Gateway의 기술 계약을 맞춘다.
- Kafka 토픽 이름은 기존 계약을 유지한다.
- 총액은 클라이언트가 아니라 서버에서 계산한다.
- Alternative를 AI 추천이라고 과장하지 않고 규칙 기반 조회라고 설명한다.
- 승인과 반려 두 경로를 모두 검증한다.

### Sprint Review 시나리오

1. `ELECTRONICS_IOT` 카테고리로 구매 요청을 시작한다.
2. Raspberry Pi와 Arduino 대체재를 확인한다.
3. Jetson 키트의 링크, 단가, 수량, 사유를 제출한다.
4. Budget PENDING과 서버 계산 총액을 확인한다.
5. 운영진이 예산을 승인한다.
6. Kafka 이벤트 이후 Request가 ACTIVE로 변경되는지 확인한다.
7. 별도 요청을 반려한다.
8. Budget FAILED와 Request REJECTED를 확인한다.

### Sprint 2 진척도

| Sprint | 계획 | Done | 미완료 | 완료율 |
|---|---:|---:|---:|---:|
| Sprint 1 | 24SP | 24SP | 0SP | 100% |
| Sprint 2 | 30SP | 26SP | 4SP | 86.7% |

남은 항목은 다음과 같다.

- N-01, 2SP: 변경된 서비스명으로 컨테이너를 재기동하고 Eureka와 Gateway 연결을 확인한다.
- D-01, 2SP: 실제 화면 캡처를 수집한다.

기능 코드가 존재하는 것과 실행 환경 및 화면 증거까지 포함한 Increment가 Done인 것은 구분해야 한다. 컨테이너 재기동, Gateway E2E, Eureka 등록, 실제 화면 캡처가 끝나기 전까지 Sprint 2 전체를 완료했다고 표현하지 않는다.

### Sprint 2 Retrospective

| 구분 | 내용 |
|---|---|
| Keep | 기존 Payment와 Kafka 골격을 Budget 승인 흐름으로 재해석해 변경량을 줄임 |
| Keep | 승인과 반려 두 경로를 모두 구현하고 검증함 |
| Problem | 논리 서비스명, Eureka 등록명, Gateway 고정 라우트 사이에 충돌이 발생함 |
| Problem | 실제 화면 캡처와 외부 Review 피드백이 부족함 |
| Problem | 공유 DB와 단일 테넌트 구조는 완성형 SaaS로 확장하기에 부족함 |
| Try | Eureka 등록명과 Gateway 호환성을 짧은 Spike로 검증함 |
| Try | 실제 데모 리허설에서 화면 증거와 외부 피드백을 수집함 |
| Try | 멀티테넌시, 권한, Outbox, AI 추천은 후속 Spike로 분리함 |

## 9. Future Backlog

현재 2일 MVP에 포함하지 않은 기능은 다음과 같다.

- 반납 처리와 재고 복구
- 날짜와 기간 기반 예약
- 동일 기간 중복 예약 방지
- 대여 연체 관리
- 파손, 수리, 분실 상태 전이
- 보증금, 연체료, 파손 배상금 정산
- 조직별 멀티테넌시와 세부 권한
- Kafka 중복 이벤트 멱등성 강화
- Outbox와 보상 트랜잭션
- 컴퓨터 비전을 이용한 재고 조사와 물품 상태 확인
- 실제 사용 데이터를 활용한 AI 추천
- 자산 변경 이력과 감사 리포트

기간별 재고 예약 기능을 구현할 때는 현재 Budget Context를 억지로 재고 예약으로 바꾸지 않고, 별도의 Stock Allocation Context가 필요한지 다시 검토한다.

## 10. 발표용 핵심 정리

사용자와 운영진의 교보재 등록, 대여, 구매 업무를 이벤트 스토밍으로 분석했다. 이벤트별 정책과 함께 변경되는 데이터를 기준으로 Member, Asset, Request, Budget, Alternative 컨텍스트를 식별하고, 기존 인프라 제약을 고려해 user, course, enrollment, payment, recommend 서비스에 매핑했다.

Sprint 1에서는 로그인부터 대여 신청, 운영진 승인, 요청 활성화, 재고 감소까지 이어지는 핵심 대여 흐름을 완성했다. Sprint Review 결과를 바탕으로 Sprint 2에서는 구매 전 대체재 확인, 서버 총액 계산, 예산 승인과 반려, Kafka 기반 요청 상태 반영으로 확장했다.

현재 MVP의 핵심 가치는 단순한 장비 목록 제공이 아니다. 조직에 흩어진 신청, 승인, 재고, 예산 기록을 하나의 흐름으로 표준화하고, 기존 자산 활용률과 운영 투명성을 높이는 것이다.
