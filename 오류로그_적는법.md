# 현업식 오류 로그 작성법

오류 로그는 단순히 에러 메시지를 모아 놓는 문서가 아니다. 다른 사람이 문서만 읽고도 다음 내용을 파악할 수 있어야 한다.

1. 어떤 문제가 발생했는가?
2. 사용자와 시스템에 어떤 영향이 있었는가?
3. 어디까지 정상이고, 어느 지점부터 실패했는가?
4. 직접 원인은 무엇인가?
5. 무엇을 변경했는가?
6. 어떤 방법으로 정상 복구를 확인했는가?
7. 같은 문제가 다시 발생하지 않게 무엇을 할 것인가?

## 1. 가장 중요한 작성 원칙

### 한 문서에는 한 장애만 기록한다

HTTP 상태 코드가 같아도 원인이 다르면 별도의 장애다.

예를 들어 다음 두 문제는 모두 503이지만 하나의 문서로 합치면 안 된다.

- 구매 요청 503: Request Service가 Asset Service를 찾지 못함
- 로그인 503: Gateway가 Eureka에서 Auth Server를 아직 찾지 못함

파일명은 다음과 같이 작성하면 찾기 쉽다.

```text
INC-2026-08-26-01-purchase-api-503.md
INC-2026-08-26-02-login-api-503.md
```

### 증상과 원인을 구분한다

```text
증상: 브라우저에서 503 Service Unavailable이 표시됨
직접 원인: Eureka에 등록된 서비스명과 호출한 서비스명이 달랐음
기여 요인: 포트 누락, URL 하드코딩, 통합 테스트 부족
```

`503이 발생했다`는 원인이 아니라 사용자가 본 증상이다.

### 확인된 사실과 추정을 구분한다

다음처럼 증거 수준을 표시한다.

| 구분 | 의미 | 예시 |
|---|---|---|
| 확인됨 | 로그, 코드, 명령어 결과로 확인 | Gateway 로그에 `No servers available` 기록 |
| 재구성 | 여러 증거를 바탕으로 발생 흐름을 복원 | 호출 실패 후 예외 처리기가 503으로 변환 |
| 추정 | 아직 검증하지 않은 가설 | Docker Compose 문제일 가능성 있음 |
| 미확인 | 필요한 검증을 수행하지 않음 | 인증을 포함한 전체 구매 요청 테스트 미실행 |

추정은 사실처럼 쓰지 않는다. 추정이 틀린 것으로 확인되면 삭제하거나 결과를 함께 기록한다.

### 시간과 환경을 반드시 적는다

같은 코드는 로컬, Docker, 개발 서버에서 서로 다르게 동작할 수 있다.

```text
발생 시각: 2026-08-26 18:40 KST
발생 환경: macOS, Docker Desktop, Docker Compose
요청 주소: POST http://localhost:3000/api/enrollments/purchases
```

시간을 기록하지 못했다면 임의로 만들지 말고 `미기록`이라고 쓴다.

### 비밀번호와 개인정보는 남기지 않는다

다음 값은 로그와 문서에 그대로 복사하지 않는다.

- 비밀번호
- Access Token, Refresh Token
- API Key
- 쿠키와 세션 ID
- 주민등록번호, 전화번호 등 개인정보

필요한 경우 `Bearer ***`, `password=***`처럼 마스킹한다.

## 2. 작성 순서

### 1단계, 장애 개요

문서 첫 부분만 읽어도 전체 상황을 파악할 수 있게 작성한다.

| 항목 | 작성 내용 |
|---|---|
| 장애 ID | 날짜와 일련번호 |
| 상태 | 조사 중, 원인 확인, 모니터링, 복구 완료 |
| 환경 | 로컬, Docker, 개발, 운영 |
| 발생 시각 | 최초 오류 발생 시각과 시간대 |
| 복구 시각 | 정상 동작을 확인한 시각 |
| 영향 기능 | 로그인, 구매 요청 등 실패한 기능 |
| 영향 범위 | 전체 사용자, 일부 사용자, 개발 환경만 해당 등 |
| 오류 코드 | HTTP 상태와 핵심 예외 |
| 관련 서비스 | 실제 호출에 참여한 서비스 |

한 줄 요약은 다음 형식으로 작성한다.

```text
[어떤 서비스]가 [어떤 이유]로 [어떤 기능]을 처리하지 못해 [사용자 영향]이 발생했다.
```

예시:

```text
Request Service가 Eureka의 Asset Service 인스턴스를 찾지 못해 구매 요청 API가 503을 반환했다.
```

### 2단계, 영향 범위

영향을 실제로 확인한 범위까지만 쓴다.

좋은 예:

```text
- 구매 요청 등록이 실패했다.
- 이미 등록된 교보재 목록 조회는 정상 동작했다.
- 운영 환경에는 배포되지 않아 로컬 개발 환경에만 영향이 있었다.
```

피해야 할 예:

```text
- 모든 백엔드가 고장 났다.
- DB에는 문제가 없는 것 같다.
```

확인하지 않은 기능은 `정상`이라고 쓰지 말고 `영향 여부 미확인`이라고 적는다.

### 3단계, 정상 흐름과 실패 흐름

요청이 이동하는 순서와 실패 지점을 표시한다.

```text
정상 흐름
Vue :3000
  → Gateway :8080
  → Request :8083
  → Asset :8082
  → Budget :8084

실패 흐름
POST /api/enrollments/purchases
  → Request Service 도착
  → CourseServiceClient 호출
  → Asset Service 검색 실패  ← 최초 실패 지점
  → RuntimeException
  → GlobalExceptionHandler가 503으로 변환
  → 브라우저에 503 표시
```

브라우저에 표시된 마지막 오류보다 서버 로그의 첫 번째 실패 지점을 찾는 것이 중요하다.

### 4단계, 핵심 증거

전체 로그를 전부 붙이지 말고 원인을 판단하는 데 필요한 부분만 남긴다.

```text
요청: POST /api/enrollments/purchases
응답: HTTP 503 Service Unavailable
최초 핵심 오류: No servers available for service: asset-service
발생 서비스: request-service
```

가능하면 다음 정보도 함께 기록한다.

- 로그가 발생한 컨테이너 또는 서비스명
- 발생 시각
- 요청 경로와 HTTP 메서드
- HTTP 상태 코드
- 최초 예외 클래스와 메시지
- 관련 코드 파일과 메서드
- 확인에 사용한 명령어

### 5단계, 원인 분석

원인은 세 부분으로 나누면 명확하다.

#### 직접 원인

장애를 바로 발생시킨 기술적 원인이다.

```text
호출자는 asset-service라는 Eureka ID를 조회했지만 실제 등록 ID는 COURSE-SERVICE였다.
```

#### 기여 요인

직접 원인이 장애로 이어지기 쉽게 만든 구조적 문제다.

```text
- 내부 URL이 Java 코드에 하드코딩되어 있었다.
- Docker DNS와 Eureka 호출 방식이 섞여 있었다.
- 서비스 간 통합 테스트가 없었다.
```

#### 원인이 아니었던 항목

조사했지만 원인이 아니었던 지점도 간단히 남긴다. 다음 장애에서 같은 확인을 반복하지 않게 해준다.

```text
- Vue는 요청을 Gateway까지 정상 전달했다.
- 컨테이너는 종료되지 않고 실행 중이었다.
- Axios는 서버의 503 응답을 표시했을 뿐 원인이 아니었다.
```

### 6단계, 조치 내용

`고쳤다`라고만 쓰지 말고 어느 파일에서 무엇을 어떻게 바꿨는지 기록한다.

| 변경 위치 | 변경 전 | 변경 후 | 목적 |
|---|---|---|---|
| `WebClientConfig.java` | `@LoadBalanced` 사용 | 일반 `WebClient` 사용 | Docker DNS 직접 호출 |
| `CourseServiceClient.java` | URL 하드코딩 | 설정값 주입 | 환경별 주소 분리 |
| `application.yml` | Docker 주소 고정 | 로컬 기본값 제공 | 로컬 실행 지원 |
| `docker-compose.yml` | 호출 URL 미지정 | Docker 내부 URL 주입 | 컨테이너 간 연결 고정 |

환경별 설정은 실제 적용값까지 함께 적는다.

```text
로컬:  http://localhost:8082
Docker: http://asset-service:8082
```

### 7단계, 검증 결과

각 검증은 증명하는 범위가 다르다. 한 단계가 성공했다고 전체 기능이 성공한 것은 아니다.

| 검증 수준 | 확인하는 내용 | 성공이 의미하는 것 |
|---|---|---|
| 소스 확인 | 코드와 설정 변경 확인 | 의도한 코드가 작성됨 |
| 설정 검사 | Compose, YAML 문법 확인 | 설정 파일을 해석할 수 있음 |
| 컴파일 | Java, Python 등의 문법과 타입 확인 | 컴파일 가능한 상태 |
| 자동화 테스트 | 단위, 통합 테스트 실행 | 작성된 테스트 범위가 통과함 |
| 컨테이너 기동 | 프로세스와 상태 확인 | 컨테이너가 실행 중임 |
| 내부 연결 | 호출 컨테이너에서 대상 URL 요청 | DNS, 포트, 네트워크 연결 가능 |
| API 검증 | 실제 HTTP 요청과 응답 확인 | 해당 엔드포인트 동작 확인 |
| E2E 검증 | 로그인부터 최종 데이터 변경까지 확인 | 사용자 전체 시나리오 동작 확인 |

다음과 같이 명령어, 실제 결과, 해석을 함께 작성한다.

```text
명령어:
docker exec gearhub-request wget -q -S -O /dev/null \
  http://asset-service:8082/api/courses

실제 결과:
HTTP/1.1 200

해석:
Request 컨테이너에서 Asset Service의 DNS, 8082 포트, API 경로까지 접근 가능하다.
```

HTTP 상태는 다음처럼 해석할 수 있다.

| 상태 | 일반적인 해석 |
|---|---|
| `200`, `201` | 요청이 정상 처리됨 |
| `401` | 서버에는 도달했지만 인증이 필요함 |
| `403` | 인증되었지만 권한이 없음 |
| `404` | 서버에는 도달했지만 요청 경로가 없음 |
| `405` | 경로는 있지만 HTTP 메서드가 허용되지 않음 |
| `500` | 대상 애플리케이션 내부 오류 |
| `503` | 현재 서비스 또는 다음 서비스가 요청을 처리할 수 없음 |

예를 들어 GET 요청의 405는 네트워크 연결 증거가 될 수 있지만, 실제 POST 비즈니스 로직이 성공했다는 증거는 아니다.

### 8단계, 재발 방지

재발 방지는 `주의한다`가 아니라 완료 여부를 확인할 수 있는 작업으로 작성한다.

| 우선순위 | 조치 항목 | 완료 조건 | 담당자 | 기한 |
|---|---|---|---|---|
| P0 | 실패 기능 E2E 재검증 | 실제 사용자 흐름이 2xx로 완료됨 | TBD | TBD |
| P1 | 서비스 간 통합 테스트 추가 | 성공과 연결 실패 시나리오가 CI에서 실행됨 | TBD | TBD |
| P1 | URL 하드코딩 제거 | 서비스 URL이 모두 설정값으로 관리됨 | TBD | TBD |
| P2 | 서비스 명명 규칙 통일 | Docker 이름과 Eureka ID 대응표 작성 완료 | TBD | TBD |

## 3. 디버깅 중 기록하는 방법

원인을 찾는 동안에는 결론부터 쓰려고 하지 말고 시간순으로 기록한다. 나중에 해결되면 이 기록을 바탕으로 장애 보고서를 정리한다.

| 시각 | 확인 내용 | 실행 명령 또는 증거 | 판단 | 다음 행동 |
|---|---|---|---|---|
| 18:40 | 구매 요청 503 확인 | 브라우저 Network 탭 | 프론트 또는 백엔드 원인 미확정 | Gateway 로그 확인 |
| 18:45 | Gateway가 Request로 전달 | Gateway 로그 | Gateway 라우팅 정상 | Request 로그 확인 |
| 18:50 | Asset Service 검색 실패 | Request 로그 | 내부 서비스 호출 문제 | URL과 Eureka 등록명 비교 |
| 19:05 | 서비스명 불일치 확인 | Eureka 화면, 설정 파일 | 직접 원인 확인 | 호출 방식 수정 |
| 19:14 | Asset HTTP 200 확인 | 컨테이너 내부 요청 | 내부 연결 복구 | 실제 구매 요청 E2E 수행 |

시간을 나중에 기억해서 만들지 말고 확인한 시점에 바로 기록하는 것이 좋다.

## 4. 복사해서 사용하는 템플릿

아래 템플릿을 복사한 뒤 대괄호 부분을 실제 내용으로 교체한다.

````markdown
# [INC-YYYY-MM-DD-번호] [기능명] [오류 코드] 장애 기록

## 1. 장애 개요

| 항목 | 내용 |
|---|---|
| 상태 | [조사 중 / 원인 확인 / 모니터링 / 복구 완료] |
| 발생 환경 | [로컬 / Docker / 개발 / 운영] |
| 발생 시각 | [YYYY-MM-DD HH:mm KST / 미기록] |
| 복구 시각 | [YYYY-MM-DD HH:mm KST / 미복구] |
| 영향 기능 | [기능명] |
| 영향 범위 | [확인된 사용자 및 시스템 범위] |
| 요청 경로 | `[HTTP 메서드와 URL]` |
| 오류 | `[HTTP 상태, 예외명, 핵심 메시지]` |
| 관련 서비스 | [서비스 목록] |

### 한 줄 요약

[어떤 서비스]가 [어떤 이유]로 [어떤 기능]을 처리하지 못해 [영향]이 발생했다.

## 2. 영향 범위

- [확인된 사용자 영향]
- [확인된 시스템 영향]
- [영향 여부를 확인하지 못한 범위]

## 3. 정상 흐름과 장애 흐름

```text
정상 흐름
[요청 서비스]
  → [중간 서비스]
  → [대상 서비스]

장애 흐름
[요청]
  → [정상 지점]
  → [최초 실패 지점]
  → [사용자에게 반환된 오류]
```

## 4. 핵심 증거

```text
[발생 시각]
[서비스 또는 컨테이너명]
[최초 예외와 핵심 메시지]
```

## 5. 원인 분석

### 직접 원인

[장애를 직접 발생시킨 원인]

### 기여 요인

- [하드코딩, 설정 혼재, 테스트 부족 등]

### 원인이 아니었던 항목

- [확인했지만 원인이 아니었던 지점]

## 6. 조치 내용

| 변경 위치 | 변경 전 | 변경 후 | 목적 |
|---|---|---|---|
| [파일 또는 설정] | [기존 상태] | [변경 상태] | [변경 이유] |

## 7. 검증 결과

| 검증 단계 | 명령 또는 방법 | 실제 결과 | 판정 |
|---|---|---|---|
| 소스 확인 | [방법] | [결과] | [통과/실패] |
| 설정 검사 | [명령] | [결과] | [통과/실패] |
| 자동화 테스트 | [명령] | [결과] | [통과/실패/미실행] |
| 컨테이너 기동 | [명령] | [결과] | [통과/실패] |
| 내부 연결 | [명령] | [HTTP 상태] | [통과/실패] |
| E2E | [사용자 시나리오] | [결과] | [통과/실패/미실행] |

## 8. 타임라인

| 시각 | 관찰 내용 | 조치 | 결과 |
|---|---|---|---|
| [HH:mm KST] | [증상] | [수행한 작업] | [확인 결과] |

## 9. 재발 방지

| 우선순위 | 조치 항목 | 완료 조건 | 담당자 | 기한 |
|---|---|---|---|---|
| [P0/P1/P2] | [작업] | [검증 가능한 완료 조건] | [이름] | [날짜] |

## 10. 회고

- 잘된 점: [빠르게 확인할 수 있었던 부분]
- 개선할 점: [탐지와 복구가 늦어진 이유]
- 핵심 교훈: [다음 장애에 적용할 한 문장]
````

## 5. 작성 후 확인표

- [ ] 한 문서에 하나의 장애만 기록했는가?
- [ ] 발생 환경과 시간대를 적었는가?
- [ ] 사용자 영향과 시스템 영향을 구분했는가?
- [ ] 증상과 직접 원인을 구분했는가?
- [ ] 최초 실패 지점을 표시했는가?
- [ ] 추정을 확인된 사실처럼 쓰지 않았는가?
- [ ] 변경 파일과 설정값을 구체적으로 적었는가?
- [ ] 명령어, 실제 결과, 해석을 함께 적었는가?
- [ ] 미실행한 테스트를 성공했다고 표현하지 않았는가?
- [ ] 재발 방지 작업에 완료 조건을 적었는가?
- [ ] 비밀번호, 토큰, 개인정보를 제거했는가?

## 6. 이번 프로젝트의 작성 예시

실제 503 장애를 위 형식으로 작성한 예시는 [이민형_오류로그.md](./이민형_오류로그.md)에서 확인할 수 있다.

핵심은 다음 한 문장이다.

> 오류 로그는 에러 메시지를 많이 붙이는 문서가 아니라, 최초 실패 지점과 증거, 조치, 검증 결과를 다른 사람이 다시 확인할 수 있게 남기는 문서다.
