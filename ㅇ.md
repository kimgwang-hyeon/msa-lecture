현재 GearHub 프로젝트의 프론트엔드 UI/UX를 전반적으로 개선해줘.

중요: 프론트엔드 코드와 정적 이미지 에셋만 수정해. 백엔드 API, 데이터 모델, 인증 로직, 비즈니스 로직, API 요청 및 응답 형식은 변경하지 마. 기존 기능, 라우팅, 반응형 디자인, 접근성은 최대한 유지해야 해.

작업을 시작하기 전에 현재 프로젝트의 공통 스타일, 컴포넌트 구조, 사용 중인 아이콘 라이브러리, 홈페이지 디자인을 먼저 확인해. 이후 홈페이지를 전체 디자인의 기준으로 삼아 다른 페이지의 버튼, 카드, 아이콘, 애니메이션을 일관된 디자인 시스템으로 정리해줘.

## 전체 디자인 방향

- 불필요한 장식은 줄이고, 여백과 타이포그래피 계층을 자연스럽게 다듬어.
- 현재 홈페이지의 분위기와 시각적 언어를 다른 페이지에도 일관되게 적용해.
- 전체 UI가 지나치게 평면적으로 보이지 않도록 절제된 전환 효과와 미세한 애니메이션을 추가해.
- 보라색/핑크색 그라디언트는 사용하지 마.
- 과도한 그림자, 지나치게 큰 border-radius, 원색에 가까운 파란색을 제거해.
- 모든 요소를 똑같이 반복하는 기계적인 레이아웃은 피하고, 카드의 크기, 배치, 강조 수준 등에 약간의 변화를 줘.

## 타이포그래피

- Inter, system-ui, Roboto처럼 흔한 기본 폰트 의존을 제거해.
- 한글과 영문이 함께 표시되어도 이질감이 적은 폰트를 선택해.
- 페이지 제목, 섹션 제목, 본문, 보조 설명, 버튼 라벨의 크기와 굵기를 명확하게 구분해.
- 폰트 로딩 실패에 대비한 적절한 fallback도 설정해.

## 로고

- GearHub 로고를 노드와 엣지가 자연스럽게 연결되는 네트워크 그래프 형태로 새롭게 만들어.
    - 로고는 4276A6 색상으로 할 수 있도록 
- 여러 개의 노드가 연결되며 하나의 허브를 형성하는 모습으로, 장비, 사용자, 그룹이 연결되는 GearHub의 의미가 전달되어야 해.
- 작은 크기에서도 형태를 알아볼 수 있도록 단순하고 명확하게 설계해.
- 가능하면 선명하게 확장할 수 있는 SVG 에셋으로 제작해.
- 내비게이션, 로그인 화면 등 기존 로고가 사용되는 모든 위치에 일관되게 적용해.
- 기존 레이아웃을 깨지 않도록 가로형 로고와 심볼 단독 사용 상황을 고려해.

## 버튼

- 전체 버튼의 쨍한 파란색을 제거하고 홈페이지에 어울리는 차분한 색상과 반투명한 표면 스타일로 통일해.
- `GearHub 시작하기` 버튼은 현재보다 채도를 낮추고 적절한 투명도와 얇은 테두리를 적용해.
- `그룹 참여` 버튼도 `GearHub 시작하기` 버튼과 동일한 디자인 언어를 사용해.(버튼 디자인은 모두 통일할 수 있도록)
- 기본, hover, focus-visible, active, disabled, loading 상태를 일관되게 구현해.
- hover 시 색상만 갑자기 바꾸지 말고, 미세한 밝기 변화, 1~2px 이동, 테두리 변화처럼 절제된 피드백을 줘.
- 모든 주요 페이지의 버튼을 공통 버튼 컴포넌트 또는 공통 디자인 토큰으로 정리해.
- 버튼의 기존 이벤트, 링크, submit 동작은 절대 변경하지 마.

## 카드와 레이아웃

- 현재 유색 회색으로 혼자 튀는 카드들을 주변 UI와 어울리는 반투명 표면으로 변경해.
- 카드 배경에는 낮은 투명도, 얇은 테두리, 매우 절제된 그림자를 사용해.
- hover 또는 화면 진입 시 가벼운 이동, 테두리 강조, opacity 변화 같은 애니메이션을 적용해.
- 3개의 카드가 완전히 동일하게 반복되어 보이는 경우, 강조 카드나 콘텐츠 밀도에 따라 크기, 여백, 배치에 약간의 변화를 줘.
- 단, 정보의 중요도와 읽는 순서는 명확하게 유지해.
- 모바일에서는 자연스럽게 단일 열로 정렬되도록 기존 반응형을 유지하거나 개선해.

## 로그인 페이지

- 로그인 페이지 오른쪽 상단에 X 형태의 닫기 버튼을 추가해.
- 키보드로 접근할 수 있어야 하며 명확한 `aria-label`을 제공해.
- 닫기 버튼을 누르면 기존 화면 흐름에 가장 자연스러운 페이지로 이동하도록 현재 라우팅 구조를 확인해서 구현해.
- 로그인과 인증 기능 자체는 변경하지 마.
- 로그인 폼의 input에도 세련된 hover, focus, error, disabled 상태를 적용해.

## 회원가입 페이지와 라우팅

- 현재 로그인 화면 안에 조건부로 표시되는 회원가입 폼을 별도의 Vue 회원가입 페이지로 분리해.
- `RegisterView.vue` 등 현재 views 구조에 맞는 회원가입 화면을 만들고, Vue Router에 `/register` 라우트를 `guestOnly` 페이지로 추가해.
- 로그인 화면의 `회원가입` 버튼은 `http://localhost:8080` 또는 백엔드 HTML 화면로 이동하지 않고, Vue Router를 사용해 프론트엔드의 `/register`로 이동해야 해.
- 회원가입 페이지의 `로그인으로 돌아가기` 또는 닫기 버튼도 Vue Router를 사용해 프론트엔드 `/login`이나 홈으로 이동하게 해.
- 화면 이동은 프론트엔드 라우팅으로 처리하되, 회원가입 제출은 현재 `authApi.register` 및 `POST /api/users/register` 연동을 그대로 재사용해. API 주소, 요청 필드, 응답 처리 형식은 변경하지 마.
- 기존 이름, 이메일, 비밀번호, 역할 필드와 loading, success, error 처리를 유지하고, 필드별 label, autocomplete, 유효성 검사, 키보드 접근성을 보완해.
- 회원가입 성공 후에는 성공 메시지를 보여준 뒤 프론트엔드 `/login`으로 자연스럽게 이동하고, 실패 시에는 사용자가 입력한 값을 불필요하게 초기화하지 마.
- 로그인 페이지와 같은 브랜딩, 로고, 카드, 입력창, 버튼 디자인을 사용하고 모바일에서도 폼이 잘리지 않게 구현해.
- `/register`에 직접 접속하거나 새로고침해도 회원가입 페이지가 정상 표시되는지 확인해.
- 앞서서 구현한 페이지들과 같이 디자인을 통일해 

## 프로필과 전체 아이콘

- 프로필 아이콘을 학교 또는 캠퍼스를 연상시키는 아이콘으로 변경해.
- 특정 운영체제에서 다르게 보이는 이모지 문자보다 현재 프로젝트에서 사용하는 SVG 아이콘 라이브러리를 우선 사용해.
- `내 대여`, 그룹, 관리자, 사용자, 장비, 반납 등 전체 아이콘의 스타일을 하나의 아이콘 세트로 통일해.
- 선의 굵기, 아이콘 크기, 컨테이너 크기, 색상 규칙을 일관되게 적용해.
- 아이콘만 있는 버튼에는 반드시 접근 가능한 이름이나 `aria-label`을 제공해.

## 체크리스트 및 선택 UI

- 반납 검수 체크리스트의 기본 브라우저 체크박스가 그대로 드러나지 않도록 디자인을 개선해.
- 각 항목을 다른 버튼 및 카드와 어울리는 선택형 행 또는 세그먼트 형태로 만들어.
- 선택 여부가 체크 아이콘, 배경, 테두리, 텍스트 상태로 분명하게 구분되어야 해.
- 키보드 조작, focus-visible, disabled 상태와 스크린 리더 접근성을 유지해.
- 실제 checkbox input과 기존 폼 제출 로직은 유지하고, 시각적 표현만 커스텀해.

## 관리자 페이지

- 관리자 페이지의 원색적인 하늘색 박스를 채도가 낮은 반투명 색상으로 수정해.
- 관리자용 정보라는 구분은 유지하되 다른 페이지와 동떨어져 보이지 않게 해.
- 배경색만으로 의미를 전달하지 말고 아이콘, 제목, 테두리, 텍스트 계층도 함께 사용해.
- 표, 상태 배지, 필터, 액션 버튼 역시 전체 디자인 시스템에 맞게 정리해.

## 애니메이션

- 홈페이지에 사용된 애니메이션의 속도와 easing을 확인하고 다른 페이지에도 일관되게 적용해.
- 페이지 진입, 카드 등장, hover, 버튼 클릭, 상태 변경에 150~400ms 수준의 절제된 애니메이션을 사용해.
- 애니메이션이 과하거나 모든 요소가 동시에 움직이지 않도록 해.
- 레이아웃 이동이나 사용자의 조작을 방해하는 효과는 사용하지 마.
- `prefers-reduced-motion` 환경에서는 애니메이션을 제거하거나 최소화해.

## 구현 원칙

- 먼저 중복되는 색상, radius, shadow, spacing, transition 값을 디자인 토큰 또는 CSS 변수로 정리해.
- 버튼, 카드, 입력창, 아이콘 컨테이너 등 반복 요소는 기존 공통 컴포넌트를 우선 활용하고, 필요할 때만 공통 컴포넌트로 추출해.
- 새로운 대형 UI 프레임워크는 설치하지 마.
- 기존 프로젝트에 아이콘 라이브러리가 있다면 그대로 사용해.
- 의미 없는 div 증가와 과도한 추상화를 피하고 현재 코드 구조를 존중해.
- 텍스트 내용, API 호출, 권한 처리, 폼 데이터 구조는 변경하지 마.
- 프론트엔드 범위를 벗어나는 문제가 발견되면 임의로 백엔드를 수정하지 말고 별도로 알려줘.

## 완료 조건

작업 후 다음 항목을 확인해.

1. 로고가 노드-엣지 기반의 GearHub 심볼로 교체되었는가
2. `GearHub 시작하기`와 `그룹 참여` 버튼의 디자인이 통일되었는가
3. 전체 버튼에서 과도하게 쨍한 파란색이 제거되었는가
4. 카드에 적절한 투명도와 절제된 애니메이션이 적용되었는가
5. 로그인 페이지 오른쪽 상단에 접근 가능한 X 버튼이 추가되었는가
6. 프로필 아이콘이 학교를 나타내며 전체 아이콘 스타일이 통일되었는가
7. 반납 검수 체크리스트가 기존 기능을 유지하면서 깔끔하게 개선되었는가
8. 관리자 페이지의 원색적인 하늘색 영역이 자연스럽게 개선되었는가
9. hover, focus-visible, active, disabled 상태가 일관되게 구현되었는가
10. 모바일, 태블릿, 데스크톱 반응형이 깨지지 않는가
11. 키보드 접근성과 `prefers-reduced-motion`이 고려되었는가
12. 기존 라우팅, 폼 제출, API 연동, 인증 기능이 그대로 동작하는가
13. 로그인의 `회원가입` 버튼이 8080 백엔드 화면이 아닌 프론트엔드 `/register`로 이동하는가
14. `/register` 직접 접속, 새로고침, 회원가입 제출, 성공 후 `/login` 이동이 정상 동작하는가

수정 후에는 변경한 파일과 주요 개선사항을 페이지별로 정리하고, 실행한 프론트엔드 빌드, 린트, 테스트 결과를 정확히 알려줘. 실행하지 못한 검증은 완료했다고 표현하지 말고 이유를 함께 적어줘.
